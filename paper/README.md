# Paper artifact

`BEGF_ICASSP2027_Final.pdf` is the final paper PDF snapshot inspected for this release. It is linked from the repository README and is the source for the displayed values in `paper_results.csv`.

The supplied workspace did not contain a `main.tex` that could be proven to generate this final PDF. The paper source is therefore not rewritten or represented as finalized here. Authors can add the verified source and venue-specific build instructions later.
